# render.yaml
services:
  - type: web
    name: jesus-prayer-api
    env: python
    plan: free
    branch: main
    buildCommand: cd server && pip install -r requirements.txt
    startCommand: cd server && python ultimate_server_enhanced.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0